// Project: libv.ui, File: src/libv/ui/chrono.hpp

#pragma once

// libv
#include <libv/utility/chrono.hpp>
// std
#include <chrono>


namespace libv {
namespace ui {

// -------------------------------------------------------------------------------------------------

using clock = libv::clock;

using time_duration_f = libv::time_duration_f;
using time_duration_d = libv::time_duration_d;
using time_duration = libv::time_duration;
using time_point = libv::time_point;

// -------------------------------------------------------------------------------------------------

} // namespace ui
} // namespace libv
